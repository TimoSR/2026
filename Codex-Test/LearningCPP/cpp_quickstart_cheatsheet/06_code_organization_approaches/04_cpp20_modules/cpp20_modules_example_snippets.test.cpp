#include <cassert>

using namespace std;

#include "cpp20_modules_example_snippets.cpp"

int main() {
    // This file is intentionally documentation-only snippets.
    // Test exists to keep "one code file -> one test file" consistency.
    assert(true);
    return 0;
}
