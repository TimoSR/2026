{
  "name": "wave-simulation-benchmark",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.2.2",
  "type": "module",
  "scripts": {
    "build:wasm": "node scripts/build-wasm.mjs",
    "build:web": "tsc --noEmit && vite build",
    "build": "pnpm build:wasm && pnpm build:web",
    "build:vercel": "pnpm build:wasm && pnpm build:web",
    "dev": "vite --host 127.0.0.1",
    "dev:wasm": "pnpm build:wasm && vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "typecheck:wasm": "pnpm build:wasm && tsc --noEmit",
    "preview": "vite preview --host 127.0.0.1",
    "rust:direct": "cargo run --manifest-path rust/optimized-tensor/Cargo.toml --example direct_call"
  },
  "dependencies": {
    "complex.js": "^2.4.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^6.0.3",
    "vite": "^7.0.0"
  }
}