# mlbin_with_crates

Rust MVP for a binary ML model container using common crates.

## Commands

```bash
cargo run -- create-demo --output demo.mlbin
cargo run -- inspect demo.mlbin
cargo run -- infer demo.mlbin 1.0 2.0
```
