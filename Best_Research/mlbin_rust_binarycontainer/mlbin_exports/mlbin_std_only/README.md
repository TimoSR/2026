# mlbin_std_only

Rust MVP for a binary ML model container using only Rust stdlib.

## Commands

```bash
cargo run -- create-demo demo.mlbin
cargo run -- inspect demo.mlbin
cargo run -- infer demo.mlbin 1.0 2.0
```
