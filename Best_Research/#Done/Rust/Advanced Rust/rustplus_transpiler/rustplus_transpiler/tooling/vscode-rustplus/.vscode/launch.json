{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Rust Plus Syntax Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
