# smart_pointers_examples

`smart_pointers_examples.cpp` is the runnable code companion for smart pointer ownership patterns:
- `unique_ptr` for single owner
- `shared_ptr` for shared lifetime
- `weak_ptr` to observe shared objects without owning them

See also:
- `smart_pointers_docs.md` for deeper ownership tradeoffs

