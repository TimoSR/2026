# format_selection_profiles.cpp

## InDepth: Intent-based selection profiles

Instead of forcing users to compare every format each time, define profiles:
- default API profile
- internal high-throughput profile
- legacy enterprise profile

That gives predictable format choices and cleaner architecture decisions.
