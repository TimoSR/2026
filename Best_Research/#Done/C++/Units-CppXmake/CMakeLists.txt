# this is the build file for project PhysicsDemo
# it is autogenerated by the xmake build system.
# do not edit by hand.

# project
cmake_minimum_required(VERSION 3.28.0)
cmake_policy(SET CMP0091 NEW)
set(CMAKE_C_COMPILER "C:/Program Files/Microsoft Visual Studio/18/Community/VC/Tools/Llvm/x64/bin/clang.exe")
set(CMAKE_CXX_COMPILER "C:/Program Files/Microsoft Visual Studio/18/Community/VC/Tools/Llvm/x64/bin/clang++.exe")
set(CMAKE_CXX_SCAN_FOR_MODULES ON)
project(PhysicsDemo VERSION 0.1.0 LANGUAGES CXX)
set(CURRENT_COMPILER_ID ${CMAKE_CXX_COMPILER_ID})

# target
add_executable(AztroPhysicsTests "")
set_target_properties(AztroPhysicsTests PROPERTIES OUTPUT_NAME "AztroPhysicsTests")
set_target_properties(AztroPhysicsTests PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/build/windows/x64/debug")
add_dependencies(AztroPhysicsTests aztro_physics)
target_include_directories(AztroPhysicsTests PRIVATE
    aztro_physics/..
)
target_compile_options(AztroPhysicsTests PRIVATE
    $<$<COMPILE_LANGUAGE:C>:--target=x86_64-pc-windows-msvc>
    $<$<COMPILE_LANGUAGE:CXX>:--target=x86_64-pc-windows-msvc>
    $<$<COMPILE_LANGUAGE:CUDA>:-G>
)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(AztroPhysicsTests PRIVATE /EHsc)
elseif(CURRENT_COMPILER_ID STREQUAL "Clang")
    target_compile_options(AztroPhysicsTests PRIVATE -fexceptions)
    target_compile_options(AztroPhysicsTests PRIVATE -fcxx-exceptions)
elseif(CURRENT_COMPILER_ID STREQUAL "GNU")
    target_compile_options(AztroPhysicsTests PRIVATE -fexceptions)
endif()
set_target_properties(AztroPhysicsTests PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(AztroPhysicsTests PRIVATE cxx_std_23)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(AztroPhysicsTests PRIVATE $<$<CONFIG:Debug>:-Od>)
else()
    target_compile_options(AztroPhysicsTests PRIVATE -O0)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(AztroPhysicsTests PRIVATE -Zi)
else()
    target_compile_options(AztroPhysicsTests PRIVATE -g)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    set_property(TARGET AztroPhysicsTests PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreadedDebugDLL")
endif()
target_link_libraries(AztroPhysicsTests PRIVATE
    aztro_physics
)
target_link_directories(AztroPhysicsTests PRIVATE
    build/windows/x64/debug
)
target_link_options(AztroPhysicsTests PRIVATE
    --target=x86_64-pc-windows-msvc
)
# private sourcefiles from sourcebatch c++.build for target AztroPhysicsTests
target_sources(AztroPhysicsTests PRIVATE 
    aztro_physics/tests/main.cpp
    aztro_physics/acceleration/tests.cpp
    aztro_physics/display/tests.cpp
    aztro_physics/force/tests.cpp
    aztro_physics/length/tests.cpp
    aztro_physics/mass/tests.cpp
    aztro_physics/operations/tests.cpp
    aztro_physics/time/tests.cpp
    aztro_physics/velocity/tests.cpp
    Testing/test_context.cpp
)

# target
add_executable(PhysicsDemo "")
set_target_properties(PhysicsDemo PROPERTIES OUTPUT_NAME "PhysicsDemo")
set_target_properties(PhysicsDemo PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/build/windows/x64/debug")
add_dependencies(PhysicsDemo aztro_physics physics)
target_include_directories(PhysicsDemo PRIVATE
    physics/..
)
target_compile_options(PhysicsDemo PRIVATE
    $<$<COMPILE_LANGUAGE:C>:--target=x86_64-pc-windows-msvc>
    $<$<COMPILE_LANGUAGE:CXX>:--target=x86_64-pc-windows-msvc>
    $<$<COMPILE_LANGUAGE:CUDA>:-G>
)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(PhysicsDemo PRIVATE /EHsc)
elseif(CURRENT_COMPILER_ID STREQUAL "Clang")
    target_compile_options(PhysicsDemo PRIVATE -fexceptions)
    target_compile_options(PhysicsDemo PRIVATE -fcxx-exceptions)
elseif(CURRENT_COMPILER_ID STREQUAL "GNU")
    target_compile_options(PhysicsDemo PRIVATE -fexceptions)
endif()
set_target_properties(PhysicsDemo PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(PhysicsDemo PRIVATE cxx_std_23)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(PhysicsDemo PRIVATE $<$<CONFIG:Debug>:-Od>)
else()
    target_compile_options(PhysicsDemo PRIVATE -O0)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(PhysicsDemo PRIVATE -Zi)
else()
    target_compile_options(PhysicsDemo PRIVATE -g)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    set_property(TARGET PhysicsDemo PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreadedDebugDLL")
endif()
target_link_libraries(PhysicsDemo PRIVATE
    physics
    aztro_physics
)
target_link_directories(PhysicsDemo PRIVATE
    build/windows/x64/debug
)
target_link_options(PhysicsDemo PRIVATE
    --target=x86_64-pc-windows-msvc
)
# private sourcefiles from sourcebatch c++.build for target PhysicsDemo
target_sources(PhysicsDemo PRIVATE 
    src/main.cpp
)

# target
add_library(aztro_physics STATIC "")
set_target_properties(aztro_physics PROPERTIES OUTPUT_NAME "aztro_physics")
set_target_properties(aztro_physics PROPERTIES ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/build/windows/x64/debug")
target_compile_options(aztro_physics PRIVATE
    $<$<COMPILE_LANGUAGE:C>:--target=x86_64-pc-windows-msvc>
    $<$<COMPILE_LANGUAGE:CXX>:--target=x86_64-pc-windows-msvc>
    $<$<COMPILE_LANGUAGE:CUDA>:-G>
)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(aztro_physics PRIVATE /EHsc)
elseif(CURRENT_COMPILER_ID STREQUAL "Clang")
    target_compile_options(aztro_physics PRIVATE -fexceptions)
    target_compile_options(aztro_physics PRIVATE -fcxx-exceptions)
elseif(CURRENT_COMPILER_ID STREQUAL "GNU")
    target_compile_options(aztro_physics PRIVATE -fexceptions)
endif()
set_target_properties(aztro_physics PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(aztro_physics PRIVATE cxx_std_23)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(aztro_physics PRIVATE $<$<CONFIG:Debug>:-Od>)
else()
    target_compile_options(aztro_physics PRIVATE -O0)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(aztro_physics PRIVATE -Zi)
else()
    target_compile_options(aztro_physics PRIVATE -g)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    set_property(TARGET aztro_physics PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreadedDebugDLL")
endif()
target_link_options(aztro_physics PRIVATE
    --target=x86_64-pc-windows-msvc
)
# private sourcefiles from sourcebatch c++.build for target aztro_physics
target_sources(aztro_physics PRIVATE 
    aztro_physics/acceleration/acceleration.cpp
    aztro_physics/detail/math.cpp
    aztro_physics/detail/quantity_display.cpp
    aztro_physics/display/display.cpp
    aztro_physics/display/format.cpp
    aztro_physics/force/force.cpp
    aztro_physics/length/length.cpp
    aztro_physics/mass/mass.cpp
    aztro_physics/operations/operations.cpp
    aztro_physics/time/time.cpp
    aztro_physics/velocity/velocity.cpp
)
# public sourcefiles from sourcebatch c++.build.modules for target aztro_physics
target_sources(aztro_physics PUBLIC FILE_SET CXX_MODULES FILES
    aztro_physics/Physics.cppm
)

# target
add_library(physics STATIC "")
set_target_properties(physics PROPERTIES OUTPUT_NAME "physics")
set_target_properties(physics PROPERTIES ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/build/windows/x64/debug")
target_include_directories(physics PRIVATE
    physics/..
)
target_include_directories(physics INTERFACE
    physics/..
)
target_compile_options(physics PRIVATE
    $<$<COMPILE_LANGUAGE:C>:--target=x86_64-pc-windows-msvc>
    $<$<COMPILE_LANGUAGE:CXX>:--target=x86_64-pc-windows-msvc>
    $<$<COMPILE_LANGUAGE:CUDA>:-G>
)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(physics PRIVATE /EHsc)
elseif(CURRENT_COMPILER_ID STREQUAL "Clang")
    target_compile_options(physics PRIVATE -fexceptions)
    target_compile_options(physics PRIVATE -fcxx-exceptions)
elseif(CURRENT_COMPILER_ID STREQUAL "GNU")
    target_compile_options(physics PRIVATE -fexceptions)
endif()
set_target_properties(physics PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(physics PRIVATE cxx_std_23)
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(physics PRIVATE $<$<CONFIG:Debug>:-Od>)
else()
    target_compile_options(physics PRIVATE -O0)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    target_compile_options(physics PRIVATE -Zi)
else()
    target_compile_options(physics PRIVATE -g)
endif()
if(CURRENT_COMPILER_ID STREQUAL "MSVC")
    set_property(TARGET physics PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreadedDebugDLL")
endif()
target_link_options(physics PRIVATE
    --target=x86_64-pc-windows-msvc
)
# private sourcefiles from sourcebatch c++.build for target physics
target_sources(physics PRIVATE 
    physics/acceleration/acceleration.cpp
    physics/acceleration/display.cpp
    physics/acceleration/operators.cpp
    physics/acceleration/quantity.cpp
    physics/detail/math.cpp
    physics/detail/quantity_display.cpp
    physics/display/format.cpp
    physics/display/stream.cpp
    physics/force/display.cpp
    physics/force/force.cpp
    physics/force/operators.cpp
    physics/force/quantity.cpp
    physics/length/display.cpp
    physics/length/length.cpp
    physics/length/operators.cpp
    physics/length/quantity.cpp
    physics/mass/display.cpp
    physics/mass/mass.cpp
    physics/mass/operators.cpp
    physics/mass/quantity.cpp
    physics/operations/checked_division.cpp
    physics/operations/equations.cpp
    physics/time/display.cpp
    physics/time/operators.cpp
    physics/time/quantity.cpp
    physics/time/time.cpp
    physics/velocity/display.cpp
    physics/velocity/operators.cpp
    physics/velocity/quantity.cpp
    physics/velocity/velocity.cpp
)

