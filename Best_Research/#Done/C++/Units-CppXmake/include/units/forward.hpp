#pragma once

namespace units
{

    class Length;
    class Time;
    class Mass;
    class Velocity;
    class Acceleration;
    class Force;

    enum class LengthUnit;
    enum class TimeUnit;
    enum class MassUnit;
    enum class VelocityUnit;
    enum class AccelerationUnit;
    enum class ForceUnit;

} // namespace units
