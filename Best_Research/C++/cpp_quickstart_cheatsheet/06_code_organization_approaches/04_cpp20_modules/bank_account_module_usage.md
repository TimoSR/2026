# bank_account_module_usage

`bank_account_module_usage.cpp` is the consumer-side example for `bank_account_module.ixx`.

It focuses on:
- importing a module
- using exported types/functions without including headers

See also:
- `cpp20_modules.md` for module constraints, build flow, and toolchain caveats

